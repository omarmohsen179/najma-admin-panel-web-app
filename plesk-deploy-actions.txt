# Plesk → Git → Additional deployment actions (one line per site).
# Repo CWD is usually: ...\git repos\negma-admin
#
# Local: build both folders, commit, push:
#   npm run build:envs
#   git add build-negma build-npm && git commit -m "chore: env builds" && git push
#
# --- npm env → admin.npm.khalidelewa.com (from build-npm) ---
robocopy build-npm "..\..\admin.npm.khalidelewa.com" /MIR /E /NFL /NDL /NJH /NJS /nc /ns /np & if errorlevel 8 (exit /b 1) else (exit /b 0)
#
# --- negma env (paste only on the negma subdomain’s Git deploy; edit folder name if yours differs) ---
robocopy build-negma "..\..\admin.negma.khalidelewa.com" /MIR /E /NFL /NDL /NJH /NJS /nc /ns /np & if errorlevel 8 (exit /b 1) else (exit /b 0)
